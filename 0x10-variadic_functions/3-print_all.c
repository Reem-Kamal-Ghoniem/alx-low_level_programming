#include <stdio.h>
#include <string.h>
#include <stdarg.h>
#include "variadic_functions.h"
/**
 * print_all - function that prints all the types
 * @format: arguments
 * Return: Nothing
 */
void print_all(const char * const format, ...)
{
	if (format == null)
		return;
}
